import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  expenseCard: {
    lightBg: '#FFFFFF',
    darkBg: '#424242'
  },
  app: {
    lightBg: '##ECECEC',
    fontSize: '1em'
  }
});

export default theme;
